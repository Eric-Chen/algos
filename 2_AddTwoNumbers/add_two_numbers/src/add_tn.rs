mod add_tn {
    pub struct ListNode {
        pub val: i32,
        pub next: Option<Box<ListNode>>,
    }

    pub fn add_two_numbers(l1: *ListNode, l2: *ListNode) -> *ListNode {
        let l: *ListNode = &ListNode{};
        let r: *ListNode = l;

        while(true) {
            
        }
    }    
}